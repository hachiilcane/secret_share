require 'spec_helper'

describe "Directories" do
  describe "GET /directories" do
    it "works! (now write some real specs)" do
      pending 'some real specs'
      # get directories_path
      # response.status.should be(200)
    end
  end
end
